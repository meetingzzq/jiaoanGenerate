providers = ["python"]

[phases.install]
cmd = "pip install -r requirements.txt"

[start]
cmd = "cd backend && gunicorn api_server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 1 --preload"
